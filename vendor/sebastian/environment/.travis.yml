language: php

sudo: false

before_install:
  - composer.phar self-update

install:
  - travis_retry composer.phar install --no-interaction --prefer-source

php:
  - 5.6
  - hhvm

notifications:
  email: false
